<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:fitsSystemWindows="true"
    android:layout_height="match_parent">

    <include layout="@layout/activity_gainslog_main"
        android:layout_height="match_parent"
        android:layout_width="match_parent"/>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        android:layout_gravity="start"
        app:itemIconTint="@drawable/selector_color_navigation"
        app:itemTextColor="@drawable/selector_color_navigation"
        app:itemBackground="@drawable/selector_background"
        app:headerLayout="@layout/header_nav"
        app:menu="@menu/activity_base"/>

</androidx.drawerlayout.widget.DrawerLayout>