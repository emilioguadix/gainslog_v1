<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:name="android.intent.category.LAUNCHER"
    android:layout_height="match_parent"
    android:background="@color/colorSplash_background"
    tools:context=".SplashActivity">
    <LinearLayout android:layout_height="wrap_content" android:layout_width="wrap_content" android:orientation="vertical" android:layout_gravity="center_vertical|center_horizontal">
    <ImageView
        android:id="@+id/imageView3"
        android:layout_width="@dimen/img_splash_logo"
        android:layout_height="@dimen/img_splash_logo"
        android:contentDescription="@string/app_name"
        android:src="@drawable/ic_logo"
        android:layout_gravity="center_horizontal"/>

    <TextView
        android:id="@+id/textView"
        android:fontFamily="@font/nexarustslab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="@dimen/tV_splash_nombre"
        android:textColor="@color/colorSplash_title"
        android:layout_gravity="center_horizontal" />
    </LinearLayout>
</FrameLayout>